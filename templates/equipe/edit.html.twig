{% extends 'base.html.twig' %}

{% block title %}Modifier l'équipe{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-4">Modifier l'équipe</h1>

        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Informations sur l'équipe</h5>
                {{ include('equipe/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
            </div>
        </div>

        <div class="mt-4">
            <a href="{{ path('app_equipe_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>

        <div class="mt-4">
            {{ include('equipe/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
